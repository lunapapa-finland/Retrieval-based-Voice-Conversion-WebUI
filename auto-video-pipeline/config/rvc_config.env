# ===== rvc_config.env (Piper-style roots; no language layer) =====
# All paths are relative to auto-video-pipeline/ unless absolute.

# I/O roots
RVC_INPUT_ROOT=data/step2_wav-tts
RVC_OUTPUT_ROOT=data/step3_wav-converted
RVC_INPUT_ROOT_ADDON=data/step0_addon/tts_addon
RVC_OUTPUT_ROOT_ADDON=data/step0_addon/rvc_addon
RVC_LOG=data/step3_wav-converted/rvced.log
RVC_LOG_ADDON=data/step0_addon/rvc_addon/rvced.log

# Week folder discovery (under RVC_INPUT_ROOT, alongside 'addon')
# Example patterns: 2025w37  or  2025Week37
WEEK_GLOB=*[0-9][0-9][0-9][0-9]Week[0-9][0-9]  
WEEK_REGEX=           # optional regex (leave blank if WEEK_GLOB is enough)

# Checkpoint selection
# 1) fixed file name (preferred when stable):
RVC_CHECKPOINT=Formal_e130_s58890.pth
# 2) or auto-pick latest by prefix (leave blank to skip):
RVC_CKPT_PREFIX=
RVC_CHECKPOINT_DIR=rvc/CHECKPOINT

# Inference params (passed to infer_cli unchanged)
F0_METHOD=rmvpe
RMS_MIX_RATE=0.25
PROTECT=0.33
FILTER_RADIUS=3

# Interpreter and CLI entry
RVC_PYTHON_CMD=
RVC_INFER_CLI=rvc/infer_cli.py

# WAV file patterns
RVC_WAV_GLOBS=*.wav,*.WAV
