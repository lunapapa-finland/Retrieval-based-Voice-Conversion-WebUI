# ===== tts-piper_config.env =====
# All paths here are RELATIVE TO THE REPO ROOT unless absolute.
# Repo root is the parent of the tts_piper/ folder.

# Input scripts (text) root: data/script/<lang>/*.txt
SCRIPTS_DIR=data/script

# Piper models root: tts_piper/models/<lang>/*.onnx (+ .onnx.json)
MODELS_DIR=tts_piper/models

# Output WAV root (RVC will read from here): data/wav-tts/<lang>/*.wav
OUTPUT_DIR=data/wav-tts

# ----- Piper invocation -----
# Default (if unset) is: "<current python> -m piper"
# If you prefer the CLI, set: PIPER_CMD=piper
# Or a specific interpreter: PIPER_CMD=/path/to/python -m piper
PIPER_CMD=

# Extra Piper args (optional), e.g.: --noise-scale 0.33
PIPER_EXTRA_ARGS=

# ----- Synthesis tunables -----
# <1.0 faster, >1.0 slower
LENGTH_SCALE=0.9
# seconds between sentences
SENTENCE_SILENCE=0.4

# Text extensions to consume (comma-separated)
TEXT_EXTS=.txt
