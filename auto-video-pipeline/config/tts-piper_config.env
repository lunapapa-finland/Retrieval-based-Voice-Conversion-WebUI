# ===== tts-piper_config.env =====
# All paths are relative to project root unless absolute.

# Where your source scripts live
SCRIPTS_DIR=data/step1_scripts

# Where Piper models (.onnx + .onnx.json) live
MODELS_DIR=tts_piper/models

# Where to write TTS wavs
OUTPUT_DIR=data/step2_wav-tts

# Incremental log (records processed output dirs so re-runs skip them)
LOG_FILE=data/step2_wav-tts/pipered.log

# Languages to process (comma-separated)
LANGS=en,fi

# Week folder discovery (use glob by default; example matches: 2025w01, 2024w37)
WEEK_GLOB=*[0-9][0-9][0-9][0-9]w[0-9][0-9]
# Optional regex (used as a fallback if set). You can leave this blank safely.
WEEK_REGEX=

# Piper invocation. Leave blank to use: "<current python> -m piper"
# Example: PIPER_CMD=/path/to/venv/bin/python -m piper
PIPER_CMD=

# Extra Piper args (optional), e.g. "--noise-scale 0.33"
PIPER_EXTRA_ARGS=

# Synthesis tunables
LENGTH_SCALE=0.9
SENTENCE_SILENCE=0.4

# Text file extensions to consume
TEXT_EXTS=.txt
